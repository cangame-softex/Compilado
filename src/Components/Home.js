import React from "react";
import LayoutHome from "../Components/LayoutHome";


const Home = ({children}) => {
    return(
        <LayoutHome>

            {children}

        </LayoutHome>
    )
};

export default Home;